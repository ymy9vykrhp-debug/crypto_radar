enum AppSection {
  home,
  market,
  signals,
  journal,
  research,
  news,
  integrations,
  settings,
}

enum WorkspaceSection {
  overview,
  chart,
  structure,
  levels,
  volume,
  signal,
  why,
  journal,
  news,
}

enum MarketSectionView { explorer, scanner, workspace }
